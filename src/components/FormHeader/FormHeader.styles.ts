import { colors } from '../../../styles/foundation';

export const componentStyles = {
  container: {
    paddingTop: 30,
    backgroundColor: colors.background,
  },
  button: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 10,
    marginLeft: -10,
  },
};
